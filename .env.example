# Partner escalation emails include a link to /msp/view/[token] when set (no trailing slash).
# On Vercel, VERCEL_URL is used as a fallback if this is unset.
NEXT_PUBLIC_APP_URL=https://example.com

# Supabase (server-only): scans, pro reports, user research, landing survey experiments (/parking-ticket-text).
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
